#include "main.h"
/**
 * this program returns the value of the addition of 2 numbers
 */
int main(void)
{
	int n;
	n = add_two(24, 45);
	_putchar(n);
	_putchar('\n');

	return (0);
}

int add_two(int a, int b)
{
	return (add(89, 9));
}

