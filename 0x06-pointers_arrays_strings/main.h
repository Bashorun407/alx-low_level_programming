/*_putchar function to print to the screen*/
int _putchar(char c);

/* task 0 a function that concatenate two strings*/
char *_strcat(char *dest, char *src);

/* task 1 a function concatenates two strings up to a given number n*/
char *_strncat(char *dest, char *src, int n);

/* task 2 a function that copies a string from src to dest string up to a number n*/
char *_strncpy(char *dest, char *src, int n);

/* task 3 a function that compares two strings*/
int _strcmp(char *s1, char *s2);int _strcmp(char *s1, char *s2);

/* task 4 a function that reverses the content of an array of integers*/
void reverse_array(int *a, int n);

/* task 5 a function that changes all lowercase letters of a string to uppercase*/
char *string_toupper(char *);

/* task 6 a function that capitalizes all words of a string*/
char *cap_string(char *);

/* task 7 a function that encodes a string into 1337*/
char *leet(char *);

/* task 8 a functiion that encode a string using rot13 */
char *rot13(char *);

/* task 9 a function that prints an integer*/
void print_number(int n);

/* task 11 a function that adds two number*/
char *infinite_add(char *n1, char *n2, char *r, int size_r);

/* task 12 a function that prints a buffer*/
void print_buffer(char *b, int size);

